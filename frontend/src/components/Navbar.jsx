function Navbar() {
  return (
    <nav className="navbar">
      <h2>KEYSTONE</h2>

      <div className="nav-links">
        <span>Dashboard</span>
        <span>Profile</span>
        <span>Logout</span>
      </div>
    </nav>
  );
}

export default Navbar;